In Rag1, we use the original files from the Nvidia official financial report as text source, then vectorize them.

In Rag2, we use the LLM model and train them with vectorized data.

In Rag3, we loaded the model into the local environment and asked it with financial question, to trace for factor.
